---
title: "Users"
description: "Create, update, delete, and look up users; manage establishment assignments and permissions for field operators."
---

The users API spans two source modules: `ListUser.js` handles user CRUD and lookup operations, while `user.js` covers establishment-assignment operations. Both target the `usuarios/` namespace (with additional calls to `clientes/`, `regiones/`, and `analytics/`).

<Note>
  All requests require a valid Bearer token. See [Authentication](/api/authentication) for setup instructions.
</Note>

---

## getClients

Fetch all clients.

**Method:** `GET`  
**Path:** `/clientes/getClients`

### Request

No parameters required.

### Response

Returns the full Axios `response` object. Access client records via `response.data`.

### Example

<CodeGroup>

```javascript getClients
import { getClients } from "@/Api/user";

const response = await getClients();
console.log(response.data);
```

</CodeGroup>

---

## getRegion

Fetch all regions.

**Method:** `GET`  
**Path:** `/regiones/getRegion`

### Request

No parameters required.

### Response

Returns `response.data` — an array of region objects.

### Example

<CodeGroup>

```javascript getRegion
import { getRegion } from "@/Api/user";

const regions = await getRegion();
console.log(regions);
```

</CodeGroup>

---

## getEstablecimientoNewCollection

Fetch the establishment list from the new Firestore sub-collection for a given user.

**Method:** `POST`  
**Path:** `/usuarios/getEstablecimientoNewCollection`

### Request

<ParamField body="uid" type="string" required>
  The Firebase UID of the user whose establishment sub-collection should be retrieved.
</ParamField>

### Response

Returns the full Axios `response` object. Access records via `response.data`.

### Example

<CodeGroup>

```javascript getEstablecimientoNewCollection
import { getEstablecimientoNewCollection } from "@/Api/user";

const response = await getEstablecimientoNewCollection("user-uid-123");
console.log(response.data);
```

</CodeGroup>

---

## getStoreNewFormat

Fetch the list of assigned establishments from the new Firestore sub-collection format. Each item includes an `id` field (the Firestore document ID) that you can use with `DeleteAsignacionEstablecimientos`.

**Method:** `POST`  
**Path:** `/usuarios/getStoreNewFormat`

### Request

<ParamField body="uid" type="string" required>
  The Firebase UID of the user.
</ParamField>

### Response

Returns the full Axios `response` object. Each record in `response.data` includes an `id` property representing the Firestore document ID.

### Example

<CodeGroup>

```javascript getStoreNewFormat
import { getStoreNewFormat } from "@/Api/user";

const response = await getStoreNewFormat("user-uid-123");
console.log(response.data);
```

</CodeGroup>

---

## DeleteAsignacionEstablecimientos

Delete a single assigned establishment from the user's Firestore sub-collection by document ID.

**Method:** `POST`  
**Path:** `/usuarios/DeleteAsignacionEstablecimientos`

### Request

<ParamField body="uid" type="string" required>
  The Firebase UID of the user.
</ParamField>

<ParamField body="idEstablecimiento" type="string" required>
  The Firestore document ID of the establishment assignment to remove. Obtain this from the `id` field returned by `getStoreNewFormat`.
</ParamField>

### Response

Returns the full Axios `response` object.

### Example

<CodeGroup>

```javascript DeleteAsignacionEstablecimientos
import { DeleteAsignacionEstablecimientos } from "@/Api/user";

const response = await DeleteAsignacionEstablecimientos(
  "user-uid-123",
  "doc-id-abc"
);
```

</CodeGroup>

---

## getStoresByRegion

Fetch all establishments available within a given region using the `sp_get_storeWithRegion` stored procedure.

**Method:** `POST`  
**Path:** `/analytics/callProcedure`

### Request

<ParamField body="method" type="string">
  Always `"sp_get_storeWithRegion"`. Set internally by the function.
</ParamField>

<ParamField body="params.nameRegion" type="string" required>
  The name of the region to filter establishments by.
</ParamField>

### Response

Returns the full Axios `response` object. Access establishment records via `response.data`.

### Example

<CodeGroup>

```javascript getStoresByRegion
import { getStoresByRegion } from "@/Api/user";

const response = await getStoresByRegion("North Zone");
console.log(response.data);
```

</CodeGroup>

---

## AsignarNewCollectionEstablecimientos

Assign one or more establishments to a user's Firestore sub-collection.

**Method:** `POST`  
**Path:** `/usuarios/AsignarNewCollectionEstablecimientos`

### Request

<ParamField body="uid" type="string" required>
  The Firebase UID of the user.
</ParamField>

<ParamField body="establecimientos" type="array" required>
  An array of establishment objects to assign to the user.
</ParamField>

### Response

Returns the full Axios `response` object.

### Example

<CodeGroup>

```javascript AsignarNewCollectionEstablecimientos
import { AsignarNewCollectionEstablecimientos } from "@/Api/user";

const response = await AsignarNewCollectionEstablecimientos(
  "user-uid-123",
  [{ id: "store-1" }, { id: "store-2" }]
);
```

</CodeGroup>

---

## DeleteMultipleAsignacionEstablecimientos

Delete multiple assigned establishments from a user's Firestore sub-collection in a single request.

**Method:** `POST`  
**Path:** `/usuarios/DeleteMultipleAsignacionEstablecimientos`

### Request

<ParamField body="uid" type="string" required>
  The Firebase UID of the user.
</ParamField>

<ParamField body="idEstablecimientos" type="array" required>
  An array of Firestore document IDs to remove from the user's assignment sub-collection.
</ParamField>

### Response

Returns the full Axios `response` object.

### Example

<CodeGroup>

```javascript DeleteMultipleAsignacionEstablecimientos
import { DeleteMultipleAsignacionEstablecimientos } from "@/Api/user";

const response = await DeleteMultipleAsignacionEstablecimientos(
  "user-uid-123",
  ["doc-id-1", "doc-id-2", "doc-id-3"]
);
```

</CodeGroup>

---

## User CRUD operations

The following functions are exported from `src/Api/ListUser.js` and handle core user management.

---

## GetUser

Fetch all users (no authentication headers).

**Method:** `GET`  
**Path:** `/usuarios/getUser`

### Request

No parameters required.

### Example

<CodeGroup>

```javascript GetUser
import { GetUser } from "@/Api/ListUser";

const response = await GetUser();
console.log(response.data);
```

</CodeGroup>

---

## getDataUser

Fetch all users with full profile data (no authentication headers).

**Method:** `GET`  
**Path:** `/usuarios/getDataUser`

### Request

No parameters required.

### Example

<CodeGroup>

```javascript getDataUser
import { getDataUser } from "@/Api/ListUser";

const response = await getDataUser();
console.log(response.data);
```

</CodeGroup>

---

## getDataUserPermission

Fetch all users with their permission data. Requires Bearer token.

**Method:** `GET`  
**Path:** `/usuarios/getDataUserPermission`

### Request

No parameters required.

### Example

<CodeGroup>

```javascript getDataUserPermission
import { getDataUserPermission } from "@/Api/ListUser";

const response = await getDataUserPermission();
console.log(response.data);
```

</CodeGroup>

---

## createUpdateUser

Create a new user or update an existing one. Requires Bearer token.

**Method:** `POST`  
**Path:** `/usuarios/createUpdateUser`

### Request

<ParamField body="data" type="object" required>
  Full user object. Include the user's `uid` to update an existing record; omit it to create a new user.
</ParamField>

### Example

<CodeGroup>

```javascript createUpdateUser
import { createUpdateUser } from "@/Api/ListUser";

const response = await createUpdateUser({
  nombre: "Ana",
  apellido: "García",
  cedula: "12345678",
  email: "ana.garcia@example.com",
  cliente: ["ClienteA"],
  region: ["Norte"],
});
```

</CodeGroup>

---

## createUserFromPreRegister

Activate a pre-registered user into a full account.

**Method:** `POST`  
**Path:** `/usuarios/createUserFromPreRegister`

### Request

<ParamField body="data" type="object" required>
  Pre-registration payload including client, region, profile, and establishment assignments.
</ParamField>

### Example

<CodeGroup>

```javascript createUserFromPreRegister
import { createUserFromPreRegister } from "@/Api/ListUser";

const response = await createUserFromPreRegister(preRegisterPayload);
```

</CodeGroup>

---

## updateUserDeleted

Soft-delete a user by marking them as deleted.

**Method:** `POST`  
**Path:** `/usuarios/updateUserDeleted`

### Request

<ParamField body="data" type="object" required>
  User object or identifier to mark as deleted.
</ParamField>

### Example

<CodeGroup>

```javascript updateUserDeleted
import { updateUserDeleted } from "@/Api/ListUser";

const response = await updateUserDeleted({ uid: "user-uid-123" });
```

</CodeGroup>

---

## deleteUser

Permanently delete a user by UID.

**Method:** `PUT`  
**Path:** `/usuarios/deleteUser`

### Request

<ParamField body="uid" type="string" required>
  Firebase UID of the user to delete.
</ParamField>

### Example

<CodeGroup>

```javascript deleteUser
import { deleteUser } from "@/Api/ListUser";

const response = await deleteUser("user-uid-123");
```

</CodeGroup>

---

## getaByCedula

Look up a user by their national ID (cédula).

**Method:** `POST`  
**Path:** `/usuarios/getaByCedula`

### Request

<ParamField body="cedula" type="string" required>
  The user's national ID number.
</ParamField>

### Example

<CodeGroup>

```javascript getaByCedula
import { getaByCedula } from "@/Api/ListUser";

const response = await getaByCedula("12345678");
console.log(response.data);
```

</CodeGroup>

---

## getaByKey

Look up a user by a custom key. Requires Bearer token.

**Method:** `POST`  
**Path:** `/usuarios/getaBykey`

### Request

<ParamField body="key" type="string" required>
  The lookup key for the user record.
</ParamField>

### Example

<CodeGroup>

```javascript getaByKey
import { getaByKey } from "@/Api/ListUser";

const response = await getaByKey("some-key-value");
```

</CodeGroup>

---

## getaByEmail

Look up a user by email address.

**Method:** `POST`  
**Path:** `/usuarios/getaByEmail`

### Request

<ParamField body="email" type="string" required>
  The user's email address.
</ParamField>

### Example

<CodeGroup>

```javascript getaByEmail
import { getaByEmail } from "@/Api/ListUser";

const response = await getaByEmail("ana.garcia@example.com");
```

</CodeGroup>

---

## Get_user_with_permissions

Fetch the permissions object for a user by email.

**Method:** `POST`  
**Path:** `/analytics/extras/getUserPermissions`

### Request

<ParamField body="userEmail" type="string" required>
  The email address of the user whose permissions to retrieve.
</ParamField>

### Example

<CodeGroup>

```javascript Get_user_with_permissions
import { Get_user_with_permissions } from "@/Api/ListUser";

const response = await Get_user_with_permissions("ana.garcia@example.com");
console.log(response.data);
```

</CodeGroup>

---

## Update_user_with_permissions

Update the permissions object for a user.

**Method:** `POST`  
**Path:** `/analytics/extras/updateUserPermissions`

### Request

<ParamField body="info" type="object" required>
  Permissions payload including the user email and updated permission flags.
</ParamField>

### Example

<CodeGroup>

```javascript Update_user_with_permissions
import { Update_user_with_permissions } from "@/Api/ListUser";

const response = await Update_user_with_permissions({
  userEmail: "ana.garcia@example.com",
  permisos: { dashboard: true, inventario: false },
});
```

</CodeGroup>
